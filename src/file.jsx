// uploading files to site, need to also create a url for the image as well.
// to display it, get the file, create a url to display it

imgage: null

accept=".jpeg .giff"

const file = Array.from(event.target.files)[0]